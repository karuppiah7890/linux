```bash
sudo apt update

sudo apt install forkstat

sudo forkstat

ulimit -a

ulimit -u

ulimit -u 1000

cat /etc/security/limits.conf

sudo vi /etc/security/limits.conf

:(){ :|:& };:

bomb(){ bomb|bomb& };bomb

_(){ _|_& };_
```