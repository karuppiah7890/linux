* What is `linux32` command? What is `linux64` command?
    - What is `setarch` command? Both `linux32` and `linux64` seem to point to `setarch` command

* What are personality flags? This is mentioned in `setarch` command help and command manual
